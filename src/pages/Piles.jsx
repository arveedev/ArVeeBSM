// Piles — interactive spatial grid map (Phase 7 / future build).
// The AI/SIA monitor lives on Home for regular users, not here.

function Piles() {
  return (
    <div className="min-h-screen px-4 pb-24 pt-6">
      <h1 className="text-xl font-semibold text-white">Piles</h1>
      <p className="mt-2 text-sm text-neutral-400">
        The interactive spatial pile map will be implemented in a future phase.
      </p>
    </div>
  )
}

export default Piles
